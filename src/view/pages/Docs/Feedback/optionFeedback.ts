// Constants
import { DOCS } from '../../../../init';

// Types
import { TypesOption } from '../types';

export const optionFeedback: TypesOption = {
    rootRoutePath: '/feedback',

    navLink: {
        path:     `/${DOCS}/feedback`,
        textLink: {
            id:   'feedback',
            text: 'Feedback',
        },
    },
};
