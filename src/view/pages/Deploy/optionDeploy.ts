// Types
import { TypesOption } from '../types';

export const optionDeploy: TypesOption = {
    rootRoutePath: '/deploy',

    navLink: {
        path:     '/deploy',
        textLink: {
            id:   'deploy',
            text: 'Deploy',
        },
    },
};
