// Core
import { lazy } from 'react';

import { option__paragraphName__(pascalCase) } from './option__paragraphName__(pascalCase)';

export const __paragraphName__(pascalCase) = {
    ComponentLazy: lazy(() => import(/* webpackChunk__paragraphName__(pascalCase): "__paragraphName__(pascalCase)" */ './ComponentMarkdown')),
    option:        option__paragraphName__(pascalCase),
};
