// Core
import { lazy } from 'react';

import { option__name__(pascalCase) } from './option__name__(pascalCase)';

export const __name__(pascalCase) = {
    ComponentLazy: lazy(() => import(/* webpackChunk__name__(pascalCase): "__name__(pascalCase)" */ './ComponentMarkdown')),
    option:        option__name__(pascalCase),
};
